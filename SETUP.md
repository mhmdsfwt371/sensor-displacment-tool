# دليل التنصيب — خطوة بخطوة

كل خطوة مكتوبة بالتفصيل، وفي آخر كل مرحلة **علامة نجاح** تتأكد بيها إنك مشيت صح
قبل ما تكمّل. لو حاجة مامشيتش، شوف قسم **حل المشاكل** في الآخر.

---

## المرحلة صفر — جهّز الملفات (٥ دقايق)

اعمل فولدر على جهازك باسم `sensor-tool` وحط جواه الملفات دي:

```
sensor-tool/
├── index.html          ← الأداة كاملة
├── README.md           ← تعريف المستودع
├── SETUP.md            ← الملف ده
├── firestore.rules     ← قواعد الأمان
├── firebase.json       ← إعداد الاستضافة (اختياري)
├── .gitignore          ← ملفات ما تترفعش
└── .nojekyll           ← ملف فاضي مهم لجيت هب
```

> `.gitignore` و`.nojekyll` بيبدأوا بنقطة. في ويندوز لو ما ظهروش:
> `View` ← فعّل `Hidden items`. في ماك: `Cmd + Shift + .`

**افتح `index.html` بالضغط عليه مرتين** — الأداة المفروض تشتغل على طول في المتصفح.

> ✅ **علامة نجاح:** الأداة فتحت وشغالة محليًا. لو الخطوة دي مشيت، أنت فعليًا
> عندك أداة شغالة دلوقتي. الباقي كله **استضافة وحسابات** — مش تشغيل.

---

## المرحلة ١ — اختار طريقة النشر

قبل ما تبدأ، اختار واحدة من التلاتة:

| الطريقة | المستودع | مين يشوف الكود | مين يشوف الأداة | التكلفة |
|---|---|---|---|---|
| **أ** — جيت هب بيدجز | عام | أي حد | أي حد بالرابط | مجاني |
| **ب** — استضافة فايربيز | خاص | فريقك بس | أي حد بالرابط | مجاني |
| **ج** — ملف على الأجهزة | خاص | فريقك بس | اللي عنده الملف | مجاني |

- **لو هتبدأ بسيط دلوقتي:** اختار **ج** — كل واحد يشغّل الملف من جهازه.
  بس خد بالك: نظام الدخول **مش هيشتغل** من ملف محلي (المتصفح بيمنع تحميل
  مكتبات فايربيز من `file://`). يعني ج = وضع محلي بالملفات فقط.
- **لو عايز دخول بيوزر وباسورد:** لازم **أ** أو **ب**.
- **لو الكود ما ينفعش يبقى عام:** اختار **ب**.

> ⚠️ لو اخترت **أ** (مستودع عام)، افتح `index.html` وغيّر السطر ده:
> ```js
> const ALLOW_OFFLINE=false;   // بدل true
> ```
> عشان ما حدش يفتح الرابط ويستخدم الأداة من غير حساب.

---

## المرحلة ٢ — الرفع على جيت هب

### ٢-أ: حساب ومستودع

1. ادخل [github.com](https://github.com) ← `Sign up` لو معندكش حساب.
2. من أعلى اليمين اضغط `+` ← `New repository`.
3. املا:
   - **Repository name:** `sensor-tool`
   - **Description:** أداة توزيع الحساسات والجيت واي
   - **Public** أو **Private** حسب اختيارك في المرحلة ١
   - **ما تفعّلش** `Add a README file` — عندنا واحد جاهز
4. `Create repository`

> ✅ **علامة نجاح:** ظهرت لك صفحة فيها أوامر جيت وعنوان المستودع.

### ٢-ب: رفع الملفات — الطريقة السهلة (من المتصفح)

1. في صفحة المستودع الفاضي اضغط `uploading an existing file`
2. **اسحب كل الملفات** من الفولدر ورميها في الصفحة
3. تحت، في خانة الرسالة اكتب: `أول نسخة`
4. `Commit changes`

> ⚠️ الرفع من المتصفح أحيانًا بيتجاهل الملفات اللي بتبدأ بنقطة.
> لو `.nojekyll` ما ظهرش في قايمة الملفات بعد الرفع:
> `Add file` ← `Create new file` ← اكتب في الاسم `.nojekyll` ← سيبه فاضي ←
> `Commit changes`. نفس الكلام لـ `.gitignore`.

### ٢-ج: رفع الملفات — من سطر الأوامر

لو مركّب جيت على جهازك:

```bash
cd sensor-tool
git init
git add .
git commit -m "أول نسخة من أداة توزيع الحساسات"
git branch -M main
git remote add origin https://github.com/USERNAME/sensor-tool.git
git push -u origin main
```

بدّل `USERNAME` باسمك على جيت هب. أول مرة هيطلب دخول — استخدم
`Personal access token` مش كلمة السر:
`Settings` ← `Developer settings` ← `Personal access tokens` ←
`Tokens (classic)` ← `Generate new token` ← فعّل `repo` ← انسخ التوكن
واستخدمه مكان كلمة السر.

> ✅ **علامة نجاح:** بترجع لصفحة المستودع وتلاقي الملفات كلها ظاهرة.

---

## المرحلة ٣ — تشغيل الرابط (طريقة أ فقط)

1. جوّه المستودع: `Settings` (التبويب العلوي)
2. من القايمة الجانبية: `Pages`
3. تحت **Build and deployment**:
   - Source: `Deploy from a branch`
   - Branch: `main` ← فولدر: `/ (root)`
4. `Save`
5. استنى دقيقتين لتلاتة، اعمل تحديث للصفحة

الرابط هيظهر فوق بالشكل ده:

```
https://USERNAME.github.io/sensor-tool/
```

> ✅ **علامة نجاح:** فتحت الرابط من الموبايل والأداة اشتغلت.
> لو ظهرت صفحة `404`، استنى كمان ٣ دقايق وجرّب — أول نشر بياخد وقت.

---

## المرحلة ٤ — فايربيز: إنشاء المشروع

> تخطى المرحلة دي كلها لو اخترت طريقة **ج** أو لسه مش محتاج نظام دخول.

1. ادخل [console.firebase.google.com](https://console.firebase.google.com)
   بحساب جوجل بتاع الشركة (مش حساب شخصي — الحساب ده هيملك البيانات)
2. `Create a project`
3. الاسم: `sensor-tool` ← `Continue`
4. **Google Analytics:** اقفلها (مش محتاجها) ← `Create project`
5. استنى لحد ما يخلص ← `Continue`

> ✅ **علامة نجاح:** دخلت على لوحة المشروع.

---

## المرحلة ٥ — تفعيل الدخول

1. من القايمة الجانبية: `Build` ← `Authentication`
2. `Get started`
3. تبويب `Sign-in method`
4. اضغط **Email/Password**
5. فعّل السويتش الأول (**Email/Password**)
   — سيب `Email link` مقفول
6. `Save`

> ✅ **علامة نجاح:** **Email/Password** ظهرت في القايمة بحالة `Enabled`.

---

## المرحلة ٦ — قاعدة البيانات

1. `Build` ← `Firestore Database`
2. `Create database`
3. **Location:** اختار `eur3` أو `europe-west` (الأقرب لمصر) ← `Next`
4. اختار **Start in production mode** ← `Enable`
5. استنى تجهيز القاعدة
6. روح تبويب `Rules`
7. **امسح اللي موجود** والصق اللي في ملف `firestore.rules`
8. `Publish`

> ✅ **علامة نجاح:** فوق الرولز مكتوب آخر تعديل بتاريخ النهارده.

---

## المرحلة ٧ — نسخ بيانات الربط

1. اضغط ترس ⚙️ جنب `Project Overview` ← `Project settings`
2. انزل تحت لقسم **Your apps**
3. اضغط أيقونة الويب `</>`
4. **App nickname:** `sensor-tool` — **ما تفعّلش** Firebase Hosting دلوقتي
5. `Register app`
6. هتظهر لك كتلة كود. اللي يهمك منها الأربع سطور دي:

```js
const firebaseConfig = {
  apiKey: "AIzaSy...",              ← انسخها
  authDomain: "sensor-tool.firebaseapp.com",   ← انسخها
  projectId: "sensor-tool",         ← انسخها
  storageBucket: "...",
  messagingSenderId: "...",
  appId: "1:123456:web:abc123"      ← انسخها
};
```

7. افتح `index.html` بأي محرر نصوص، دوّر على أول السكربت وحط القيم:

```js
const FIREBASE={
  apiKey:"AIzaSy...",
  authDomain:"sensor-tool.firebaseapp.com",
  projectId:"sensor-tool",
  appId:"1:123456:web:abc123"
};
const ALLOW_OFFLINE=false;
```

8. احفظ الملف وارفعه تاني على جيت هب (اسحبه في `Add file` ← `Upload files`،
   أو `git add . && git commit -m "ربط فايربيز" && git push`)

> ⚠️ **مهم:** الأربع قيم دي **مش أسرار**. أي تطبيق ويب بيعرضها في المتصفح،
> والحماية بتيجي من قواعد فايرستور والنطاقات المسموحة. عادي تتحط في المستودع.
> اللي **سرّي فعلًا** هو **مفتاح حساب الخدمة** (ملف ينتهي بـ
> `-firebase-adminsdk-....json`) — ده ما يترفعش أبدًا، و`.gitignore` بيمنعه.

> ✅ **علامة نجاح:** فتحت الرابط ولقيت **شاشة دخول** بدل الأداة.

---

## المرحلة ٨ — السماح للنطاق

من غير الخطوة دي الدخول هيفشل حتى لو البيانات صح.

1. `Authentication` ← تبويب `Settings`
2. `Authorized domains` ← `Add domain`
3. اكتب: `USERNAME.github.io` (من غير `https://` ومن غير اسم المستودع)
4. `Add`

> ✅ **علامة نجاح:** النطاق ظهر في القايمة تحت `localhost`.

---

## المرحلة ٩ — فتح حسابات الموظفين

1. `Authentication` ← تبويب `Users`
2. `Add user`
3. البريد وكلمة سر مؤقتة (٦ حروف على الأقل)
4. `Add user`
5. كرّر لكل موظف

**بلّغ الموظف:** يدخل بالبريد وكلمة السر المؤقتة، وبعدين يستخدم
**نسيت كلمة السر** عشان يحطّ واحدة من عنده.

> مفيش تسجيل ذاتي في الأداة **بقصد** — الحسابات بتتفتح من الإدارة بس.

> ✅ **علامة نجاح:** دخلت بحساب من اللي عملتهم والأداة فتحت، واسم البريد
> ظهر فوق جنب زر **خروج**، وظهر قسم **مشاريع السحابة**.

---

## المرحلة ١٠ — اختبار التسليم

امشي على القايمة دي بالترتيب:

- [ ] فتحت الرابط من موبايل ومن كمبيوتر
- [ ] سجّلت دخول بحساب موظف
- [ ] رفعت صورة رسمة حقيقية
- [ ] دوست **شغّل كل حاجة** وجاوبت على سؤال المتر
- [ ] الغرف اتحددت والحساسات اتوزعت
- [ ] زوّدت ونقّصت عدد في غرفة من الزراير + و −
- [ ] **فحص قبل التسليم** طلع سليم أو ملاحظات مفهومة
- [ ] **حفظ للسحابة** ← ظهرت رسالة نجاح
- [ ] عملت خروج ودخلت بحساب تاني ← **فتح من السحابة** ← لقيت المشروع
- [ ] **حفظ صورة الرسمة** ← الصورة فيها الترويسة والمفتاح والأعداد
- [ ] **حفظ ملف للطباعة** ← اخترت «حفظ كملف» وطلعت الأوراق صح
- [ ] **نسخ ملخص للمبيعات** ← لزقته في رسالة
- [ ] بدّلت اللغة للإنجليزي ورجّعت

> ✅ لو كل البنود عدّت، الأداة جاهزة تتسلّم للأقسام.

---

## تعديل الأداة بعدين

أي تعديل في `index.html`:

**من المتصفح:** افتح الملف في المستودع ← أيقونة القلم ✏️ ← عدّل ←
`Commit changes`. التحديث بيظهر على الرابط خلال دقيقتين.

**من سطر الأوامر:**
```bash
git add .
git commit -m "وصف التعديل"
git push
```

---

## حل المشاكل

| المشكلة | السبب | الحل |
|---|---|---|
| الرابط بيدي `404` | النشر لسه شغال | استنى ٥ دقايق واعمل تحديث |
| صفحة بيضا فاضية | الملف مش اسمه `index.html` | غيّر الاسم بالظبط بحروف صغيرة |
| `auth/unauthorized-domain` | النطاق مش مسموح | المرحلة ٨ |
| `auth/invalid-api-key` | المفتاح ناقص أو فيه مسافة | راجع النسخ في المرحلة ٧ |
| `auth/invalid-credential` | البريد أو الباسورد غلط | جرّب **نسيت كلمة السر** |
| شاشة الدخول مش بتظهر | `apiKey` فاضي | املا القيم في المرحلة ٧ |
| «السحابة مش متظبطة» | قواعد فايرستور | المرحلة ٦ خطوة ٧ |
| «الملف كبير على السحابة» | صورة الرسمة تقيلة | استعمل **تحويل لخطوط** قبل الحفظ |
| الدخول مش شغال من الملف المحلي | المتصفح بيمنع `file://` | لازم رابط — طريقة أ أو ب |
| الاكتشاف مش لاقي غرف | الرسمة باهتة | **تحويل لخطوط** الأول، وبعدين اكتشاف |
| الغرف بتتلزق ببعض | أبواب مفتوحة في الرسمة | استعمل **مستطيل** للغرف دي |

---

## استضافة فايربيز — طريقة ب

لو اخترت المستودع الخاص:

```bash
npm install -g firebase-tools
firebase login
cd sensor-tool
firebase use --add        # اختار مشروعك
firebase deploy
```

`firebase.json` جاهز في الفولدر. الرابط هيبقى:
`https://اسم-المشروع.web.app`

وساعتها ضيف النطاق ده كمان في **Authorized domains** (المرحلة ٨).
